// 合并reducers
import { combineReducers } from 'redux';
function wyf(prevState={}, action) {
  switch (action.type) {
    default:
      return prevState;
  }
}
export default combineReducers({
  wyf,
});
